<%= form_with model: @post, url: sort_select_posts_path, method: :get, local: true do |f| %>
  <%= f.select :keyword, [
    ['投稿が新しい順', 'new'],
    ['投稿が古い順', 'old'],
    ['共感が多い順', 'empathy'],
    ['共感が少ない順', 'noempathy']
  ] %>
  <%= f.submit "並べ替える" %>
<% end %>
