<div class="container mx-auto">
  <%= form_with model: @post, local: true do |f| %>
    <div class="mb-2">
      <p>タグを入力(タグとタグの間にカンマを入力してください)</p>
      <p>例: 転職,新人,営業 </p>
      <%= f.text_field :name, class: "border-2 w-full py-1 px-3" %>
    </div>
    <div>
      <p>悩みを入力してください</p>
      <%= f.text_area :text, class: "border-2 w-full h-48 p-3" %>
    </div>
    <div class="text-right">
      <%= f.submit "新規投稿", class: "bg-yellow-500 p-2 text-white cursor-pointer hover:bg-yellow-700" %>
    </div>
  <% end %>
</div>
